services:
  nginx:
    build: ./requirements/nginx/.
    container_name: nginx
    ports:
      - 443:443
      - 80:80
    # networks:
    #   - docker-network
  wordpress:
    build: ./requirements/wordpress/.
    container_name: wp-php

# networks:
#   docker-network:
#     name: docker-network